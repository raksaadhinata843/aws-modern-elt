athena:
  target: dev
  outputs:
    dev:
      type: athena
      s3_staging_dir: "{{ env_var('DBT_S3_STAGING_DIR') }}"
      region_name: "{{ env_var('AWS_REGION') }}"
      database: awsdatacatalog # Ini biasanya tetap
      schema: "{{ env_var('DBT_SCHEMA') }}"
      threads: 1
